# singularity-opencanary.conf
# Example OpenCanary config (JSON-like YAML)
{
  "device.node_id": "singularity",
  "log": {
    "type": "file",
    "filename": "/var/log/opencanary/opencanary.log"
  },
  "services": {
    "ssh": {"enabled": true},
    "ftp": {"enabled": false},
    "http": {"enabled": true}
  }
}
